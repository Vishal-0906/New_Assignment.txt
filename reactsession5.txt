Session 5 – Event Handling
Question 1
Create a React component called LikeButton that displays a button and a count. When the button is clicked, increment the count and update the display.
Answer
JSX
import React, { useState } from "react";

function LikeButton() {
  const [count, setCount] = useState(0);

  const handleClick = () => {
    setCount(count + 1);
  };

  return (
    <div>
      <h2>Likes: {count}</h2>
      <button onClick={handleClick}>Like 👍</button>
    </div>
  );
}

export default LikeButton;
Question 2
Build a SearchBar component where the user can type a product name. As the user types in the input box, display the current value below in real time using React state.
Answer
JSX
import React, { useState } from "react";

function SearchBar() {
  const [search, setSearch] = useState("");

  return (
    <div>
      <input
        type="text"
        placeholder="Search Product"
        value={search}
        onChange={(e) => setSearch(e.target.value)}
      />

      <h3>Searching: {search}</h3>
    </div>
  );
}

export default SearchBar;
import React, { useState } from "react";

function LoginForm() {
  const [username, setUsername] = useState("");
  const [password, setPassword] = useState("");

  const handleSubmit = (e) => {
    e.preventDefault();

    alert(`Username: ${username}\nPassword: ${password}`);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input
        type="text"
        placeholder="Username"
        value={username}
        onChange={(e) => setUsername(e.target.value)}
      />

      <br /><br />

      <input
        type="password"
        placeholder="Password"
        value={password}
        onChange={(e) => setPassword(e.target.value)}
      />

      <br /><br />

      <button type="submit">Login</button>
    </form>
  );
}

export default LoginForm;
Question 3
Create a simple LoginForm component with two input fields: username and password. When the form is submitted, display an alert with the entered username and password.
Answer
JSX
import React, { useState } from "react";

function PlaylistAdder() {
  const [song, setSong] = useState("");
  const [artist, setArtist] = useState("");
  const [playlist, setPlaylist] = useState([]);

  const addSong = (e) => {
    e.preventDefault();

    setPlaylist([
      ...playlist,
      {
        song,
        artist,
      },
    ]);

    setSong("");
    setArtist("");
  };

  return (
    <div>
      <form onSubmit={addSong}>
        <input
          type="text"
          placeholder="Song"
          value={song}
          onChange={(e) => setSong(e.target.value)}
        />

        <input
          type="text"
          placeholder="Artist"
          value={artist}
          onChange={(e) => setArtist(e.target.value)}
        />

        <button>Add</button>
      </form>

      <ul>
        {playlist.map((item, index) => (
          <li key={index}>
            {item.song} - {item.artist}
          </li>
        ))}
      </ul>
    </div>
  );
}

export default PlaylistAdder;
Question 4
Build a PlaylistAdder component for a Spotify-style app: allow the user to enter a song name and artist, and when the form is submitted, add the song to a displayed list below the form.
Answer
JSX
Question 5
Refactor your LoginForm to clear the input fields after successful form submission.
Answer
const handleSubmit = (e) => {
  e.preventDefault();

  alert(`Username: ${username}\nPassword: ${password}`);

  setUsername("");
  setPassword("");
};
